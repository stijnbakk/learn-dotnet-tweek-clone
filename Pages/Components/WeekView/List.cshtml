@model List<tweekClone.Models.Item>;

<ul class="sortable" hx-post="/?handler=SortItems" hx-trigger="end" hx-include="closest ul">
    @foreach (var item in @Model)
        {
            @await Html.PartialAsync("/Pages/Components/WeekView/ListItem.cshtml", item)
        }
</ul>